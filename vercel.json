{
  "buildCommand": "npm run build && npm run docs:build",
  "outputDirectory": "dist",
  "rewrites": [
    { 
      "source": "/docs/:path*",
      "destination": "/docs/.vitepress/dist/:path*" 
    }
  ]
}